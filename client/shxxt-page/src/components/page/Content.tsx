import React from "react";
import styled from "styled-components";

const ContentContainer = styled.div({
  display: "flex",
  paddingTop: "10rem",
  width: "100%",
  height: 10000,
  backgroundColor: "#00a",
});

export default function Content() {
  return <ContentContainer>asdfasdf</ContentContainer>;
}
